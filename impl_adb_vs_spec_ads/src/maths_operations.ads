package maths_operations is
   --  ads in ada is similar to .h in c language. It represents
   --  the interface that specify what the package offers.
   
   function Add (A, B : Integer) return Integer;
   function Multiply (A, B : Integer) return Integer;
   
end maths_operations;
